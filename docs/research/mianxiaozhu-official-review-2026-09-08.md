# 面小猪指定域名官方公开资料调研（页面品牌：面小助）

- **指定研究基准日期／文件名日期：2026-09-08。** 下列网页采集发生在本会话前段日期上下文为 2026-09-08 时。后续用户环境提示已到 2026-09-09；本次收尾仅整理既有证据，**没有在 2026-09-09 重新联网校验**。
- **调查对象：** 用户指定的 `https://mianxiaozhu.com.cn` 所对应公开站点。成功取得的页面实际使用品牌 **面小助**，不是“面小猪”。不将其他同名／近名产品的资料并入。
- **范围：** 按 research 技能，仅以目标域名的首页、公开 API 文档和其明确引用的 OpenAPI 描述为产品事实来源；未分析本地 interview-assistant 源码。
- **执行边界：** 本调研者未下载安装包、未运行客户端、未注册、未付费、未联系微信客服；仅读取公开网页及文档。主代理另行开展的样本静态分析不属于本报告的官网证据。
- **文件边界：** 只写本文件，没有另存完整 HTML、安装包或网络日志。本文件包含结构化摘录和必要的下载 HTML 片段，**不是完整网页快照**。

## 1. 结论速览

1. **可访问的第一方页面：** `http://mianxiaozhu.com.cn/`、`http://mianxiaozhu.com.cn/docs`、`http://mianxiaozhu.com.cn/openapi.json` 均曾通过只读 HTTP 请求取得 200 和相应正文。HTTPS 在本环境未成功读取。[S1][S7][S8]
2. **定位与功能宣传：** Windows／macOS 桌面 AI 面试辅助，包含实时语音转写及回答建议、悬浮窗、截图识题、个人材料知识库、技能卡和面试准备快照；套餐表列有模拟面试，流程说明提到面试后对话沉淀。[S2][S3][S4][S6]
3. **当前采集页面标价：** 免费试用 15 分钟；¥9.9／30 分钟；¥99／6 小时；¥199 一次性买断、无限面试时长，买断档另标明需自行配置 API Key。页面总述为非订阅时长包。[S4]
4. **自带 Key 不能简单写成“不支持”：** 官网总述不必自行配置 LLM、语音、视觉、Embedding Key，但买断档明确要求自行配置。兼容服务商、模型名、配置字段和每一类别是否必填，均未披露。[S4]
5. **隐私仅是官网承诺：** 宣称本地加密保存简历、JD、对话，数据不传云端，不用于模型训练，并提供屏幕共享／录屏防护。未做数据流、磁盘加密、模型请求或防录屏实测。[S2][S5]
6. **可核实下载按钮，不能据此证明包体：** 首页确实含 Windows `.exe` 和 macOS `.dmg` 两个相对链接；本调研未取得包体、响应头、大小、哈希、数字签名或客户端版本。[S6]
7. **不能从官网确定桌面／后端框架：** 可确认的是公开 Swagger UI 和 OpenAPI 描述，不是 Electron、Tauri、Python 或某个后端框架的产品架构证明。[S7][S8]

## 2. 证据等级与产品身份

| 等级 | 含义 | 本报告中的例子 |
| --- | --- | --- |
| **官网宣传** | 已读取官方页面上的功能／价格／隐私表述；没有证明软件真的实现或表现如何 | 实时转写、截图解题、本地加密、防录屏 |
| **实际验证（仅网页／文档层）** | 请求结果、HTML 链接、脚本静态逻辑、文档字段得到直接观察；不等于运行产品 | HTTP 200、下载 href、套餐文本、Swagger 指向的 JSON |
| **推论／解释** | 对已观察材料作有限解释，不能当成产品实现事实 | 通用“免配 Key”说明应与买断档的例外一起阅读 |
| **未知／未验证** | 未披露、证据不足，或本任务不做相应验证；不等于“不支持” | 手机协同、具体模型、客户端加密算法、系统最低版本 |

首页标题、导航、正文及页脚均使用“面小助”；域名和安装文件名含 `mianxiaozhu`。因此，本报告的对象是**用户给定域名当前呈现的产品**。无法仅由这些信息确定“面小猪”是旧名、别名还是用户笔误，也未独立核验运营主体。[S1][S6]

## 3. 官网访问、可用页面与限制

### 3.1 实际可用方式

以下命令曾成功取得完整首页 HTML，未在磁盘保存：

```powershell
Invoke-WebRequest -UseBasicParsing -Uri 'http://mianxiaozhu.com.cn/' -TimeoutSec 25
```

- 首页返回 `200`，后续复核的内容类型为 `text/html; charset=utf-8`。[S1]
- `/docs` 返回标题为“面小助 API - Swagger UI”的文档页；该页明确引用同域 `/openapi.json`。[S7]
- `/openapi.json` 返回 OpenAPI 描述，标题为“面小助 API”，description 为官网 AI 面试智能体说明。[S8]

### 3.2 访问限制

| 方式／目标 | 本次结果 | 不能据此下的结论 |
| --- | --- | --- |
| `web.run` 访问／搜索目标域名及产品名 | 多次调用未取得可用页面正文；访问遇到 Cache miss／抓取不可用情况，搜索未提供可采纳的第一方资料 | 不能说网站不存在、没有产品，或互联网上绝无帮助资料 |
| 普通受限 shell 请求 HTTPS 根域 | 报无法连接到远程服务器 | 不能归因为站点全面宕机 |
| 获准只读网络访问后的 HTTPS 根域 | 报 `Received an unexpected EOF or 0 bytes from the transport stream.` | 未定位是站点 TLS、网络出口还是中间链路问题 |
| `https://www.mianxiaozhu.com.cn/` | 报 SSL 连接无法建立 | 不证明裸域与 www 的部署和证书配置相同 |
| `http://mianxiaozhu.com.cn/` | 200，成功读取 HTML | HTTP 成功不等于 HTTPS 或安装包可用 |

**传输证据局限：** 成功资料来自用户指定域名的明文 HTTP 响应；未独立核验 TLS 证书和运营主体，不应把这一获取方式视为具有 HTTPS 的传输完整性保证。

### 3.3 公开帮助／隐私说明发现结果

- 首页的完整信任中心、服务条款、隐私政策和联系我们链接均是 `href="#"`，没有指向独立说明文件。[S1][S5]
- 对常规公开入口 `/robots.txt`、`/sitemap.xml`、`/help`、`/privacy`、`/terms` 的既有检查均返回 `200`、HTML 类型，且正文与首页字符串完全相同；**这些响应不是独立 robots、站点地图、帮助或隐私文件**。[S9]
- `/docs` 是实际不同的公开 API 文档，但内容范围主要是官网／下载服务，并非完整产品使用手册。[S7][S8]
- 未找到可确认归属本产品的其他第一方公开帮助页。这里的“未找到”仅描述本次受限调查，不排除登录后文档或其他未发现渠道。
- 已按用户要求停止进一步联网、目录入口检查和下载元数据请求。

## 4. 功能与平台：可用于对比的官方表述

以下各行的“宣称有”均仅表示页面有明确表述，**所有客户端功能均未经本调研实测**。

| 维度 | 官网可确认的表述 | 未证实范围 | 来源 |
| --- | --- | --- | --- |
| 实时语音／AI 应答 | 桌面悬浮层提供语音转文字，检索个人知识库并生成口语化回答建议 | 麦克风／系统音频来源、双通道、说话人区分、语言、延迟、准确率、ASR 厂商 | [S2][S3] |
| 会议软件配合 | 点名腾讯会议、钉钉、飞书，并称共享屏幕时面试官看不到悬浮窗 | 是否需要插件、会议软件版本、共享方式和各系统下兼容性 | [S2] |
| 截屏识题 | 从悬浮层触发截图，识别技术问题并给出分步思路 | OCR／视觉模型、算法题覆盖、多屏、权限和识别成功率 | [S2] |
| 模拟面试 | 免费、时长包和买断四档的功能列表均列出模拟面试 | 语音或文字形态、岗位题库、追问策略、评分、额度是否与实时面试共用 | [S4] |
| 简历与岗位材料 | 可导入简历、JD、项目资料，对话确认后作为回答参考 | 文件格式、大小上限、简历生成／排版／润色／评分是否另有功能 | [S2][S3] |
| 个人知识库 | 面试时检索已确认的简历、项目材料和技能资料 | 检索算法、分块、向量库、Embedding 厂商、数据同步和导出 | [S2][S3][S4] |
| 技能卡 | 通过追问整理已确认的项目事实、指标和技术取舍 | 自动抽取质量、编辑流程、版本历史 | [S2][S3] |
| 准备快照 | 将确认后的简历和技能卡打包为本轮面试的参考范围 | 快照存储结构、不可变性、隔离机制 | [S3] |
| 复盘／沉淀 | 宣称面试后对话进入技能档案，逐轮更新材料 | 未明确展示评分报告、完整录音回放、结构化复盘报告、时间轴或导出 | [S3] |
| 桌面平台 | 首页列 Windows 和 macOS，并提供对应下载按钮 | 最低系统版本、x64／ARM64、Intel／Apple Silicon、Linux 支持 | [S1][S6] |
| 双端／多端 | 能证实的是 Windows 与 macOS 两种桌面平台的宣传 | **没有足够依据确认电脑＋手机协同、远程第二屏、跨设备会话／实时同步**；不能把两种系统直接写成“手机电脑双端” | [S1][S2][S6] |

## 5. 模型、语音接入与 API Key

官网价格区总述提及四类能力：**LLM、语音、视觉、Embedding**，并说不需要用户自己配置 Key；但 ¥199 买断卡单独标明需自行配置 API Key。[S4]

- **官网宣传：** 存在开箱即用的说明，以及买断档自行配置 Key 的明确要求。把全产品写成“不允许自带 API Key”是不准确的。[S4]
- **有限解释：** 应按套餐区分，而不是把价格区总述覆盖到买断档；网页没有给出更详细的托管接入与自带 Key 规则。[S4]
- **未知：** 模型／ASR 服务商、具体模型与版本、OpenAI-compatible 接口、自定义 base URL、本地模型、各类 Key 是否全部必填、Key 保存在何处、服务端是否可见、是否可在其他套餐使用自己的 Key。
- **未知：** 自带 Key 的调用费用、限制与兼容性。不得把 ¥199 的“无限面试时长”解释成模型和语音服务也免费无限。
- OpenAPI 文档没有披露用于核实上述模型、ASR、知识库或 Key 管理行为的完整客户端业务协议；也不能因官网文档未列相关接口就说产品没有这些接口。[S8]

## 6. 定价和购买方式

**这是 2026-09-08 采集页面的展示价格，不是已完成购买或结算验证后的价格。** [S4]

| 官网套餐 | 展示价格 | 宣称时长 | 页面列出的共同功能 | Key 限制 |
| --- | --- | --- | --- | --- |
| 免费试用 | ¥0 | 15 分钟 | 模拟面试、个人知识库、技能卡、截图解题 | 未在此卡另标自配；价格区总述免配 |
| 按半小时 | ¥9.9／半小时 | 30 分钟，可续 | 同上 | 同上 |
| 标准包 | ¥99 | 6 小时 | 同上 | 同上 |
| 无限时长／买断 | ¥199，一次性 | 无限面试时长 | 同上 | **需自行配置 API Key** |

页面宣称时长包不是订阅、用完为止；下载区域另称试用不需要信用卡。[S4][S6]

**实际验证限于首页静态逻辑：** 四个套餐按钮采用 `javascript:void(0)`，携带 `data-package`；绑定函数只调用客服弹窗。弹窗文字引导添加企业微信客服咨询和购买，使用同域 `/weixin/kefu.jpg` 图片。没有执行弹窗、扫码、联系或支付，也不能从页面推断真实支付后端、支付渠道和自动开通行为。[S1][S4]

**计费未知项：** 计时开始／暂停机制、最小扣费单位、模拟面试和实时面试是否共用额度、暂停后是否扣费、余额有效期、退款、发票、设备限制、赠送活动和公平使用条款。页面宣传“用完为止”不等于已经核实了所有余额有效期规则。

## 7. 隐私、数据存储与防录屏

| 官网承诺 | 本调研证据级别 | 未证实项 |
| --- | --- | --- |
| 简历、JD、对话本地加密存储，不传云端 | 官网宣传 [S5] | 文件位置、数据库、算法、密钥管理、日志／备份／崩溃报告 |
| 个人档案和知识库仅自己的账号可访问 | 官网宣传 [S5] | 登录授权、服务端访问控制、多设备共享边界 |
| 面试数据不用于模型训练 | 官网宣传 [S5] | 法律承诺全文、第三方处理方协议、保留期限、退出／删除机制 |
| 共享屏幕隐藏悬浮层、录屏防护 | 官网宣传 [S2][S5] | 系统 API 实现、支持的录制器、整屏／窗口共享差异、失效场景 |

**重要解释：** 本地保存的宣传不能自动证明“所有推理都在本机”或“敏感内容从不传给任何模型／语音服务”。页面同时介绍 AI 接入类别及自配 Key，却没有提供数据流说明；是否传出、传给谁、如何脱敏，均需独立验证。不能只凭这些广告文字判定隐私合规、端到端加密或完全离线能力。[S4][S5]

隐私政策和服务条款按钮没有给出独立文本，本次也未取得可作为正式政策全文的其他页面。[S1][S9]

## 8. 下载链接、按钮行为与版本

### 8.1 从首页直接核实的链接

| 平台 | 首页原始 href | 按实际 HTTP 首页解析的完整 URL | 核实范围 |
| --- | --- | --- | --- |
| Windows | `/downloads/mianxiaozhu-setup.exe` | `http://mianxiaozhu.com.cn/downloads/mianxiaozhu-setup.exe` | **核实了页面存在这个链接；未验证响应或包体** |
| macOS | `/downloads/mianxiaozhu-latest.dmg` | `http://mianxiaozhu.com.cn/downloads/mianxiaozhu-latest.dmg` | **核实了页面存在这个链接；未验证响应或包体** |

来源为官网首页下载区，采集日期 2026-09-08。[S6]

必要的 HTML 摘录：

```html
<a href="/downloads/mianxiaozhu-setup.exe" class="download-btn" id="downloadWin">
<a href="/downloads/mianxiaozhu-latest.dmg" class="download-btn" id="downloadMac">
```

- 下载按钮是普通链接；首页脚本按 `navigator.platform`／`navigator.userAgent` 判断 Windows 或 macOS，只调整推荐提示和样式，未见此段逻辑改写上述 href。[S1][S6]
- 已提供的是**官方页面列出的下载地址**，不是已经验证的安装包制品。其他说明路径曾回退成首页 HTML，因此主代理获取文件时不能只以 200 判断下载成功。[S6][S9]
- 首页未展示客户端版本、发行日期、大小、哈希、签名、CPU 架构；文件名 `latest` 不能证明具体版本或新旧。[S6]
- 本调研没有得到可用的安装包 HEAD 结果。相关请求被中断，未据此编造大小或状态。

### 8.2 公开 API 文档中的补充线索

`http://mianxiaozhu.com.cn/docs` 引用 `http://mianxiaozhu.com.cn/openapi.json`；后者列有：[S7][S8]

```text
GET  /api/download/check
POST /api/download/info
GET  /api/download/public/file/{os_type}
```

- `/api/download/public/file/{os_type}` 的参数只标为字符串，没有提供可确认的取值枚举。本报告不猜测可直接使用的参数值。
- 这是**已读取的文档声明**，不是对应下载接口实际成功的证明。没有下载该接口返回物；只读更新检查的请求被中断，未得到可采用结果。
- OpenAPI 版本字段为 `3.1.0`，`info.version` 为 `0.1.0`；后者是 API 文档标识，**不能当作客户端当前版本**。[S8]

## 9. 可证实技术与严格边界

| 范围 | 有直接证据的观察 | 不应扩展成的结论 |
| --- | --- | --- |
| 官网页面 | 取得 HTML、内联 CSS／JavaScript，可阅读按钮和弹窗逻辑 | 不能据此判定桌面壳、业务后端或客户端数据存储框架 |
| 官网公开 API 文档展示 | `/docs` 使用 Swagger UI，资源 URL 指向 `swagger-ui-dist@5`；同域提供 OpenAPI 3.1.0 描述 | 文档 UI／图标／命名习惯不构成后端运行框架证明 |
| 文档覆盖范围 | 路径集中于官网页面、下载相关操作及健康检查 | 不能视为完整产品服务架构、客户端 API 清单或模型接入实现 |
| 桌面／产品后端 | **仅凭本次官网证据未知** | 不从网站前端推断 Electron、Tauri、Python、FastAPI、数据库、向量库或模型供应商 |

以上技术观察仅来自官网公开材料。[S1][S7][S8]

### 与主代理样本分析的分界

用户／主代理后续告知已获得 `E:\Project\interview_assistant\exe\mianxiaozhu-setup.exe`，称大小为 **189,814,932 bytes**，并反馈静态解包发现 **Electron + Python backend 源码**。

**这些是主代理／用户反馈，不是官网宣传，也不是本调研者的实际验证。** 本调研者未读取该文件、未计算哈希、未检查解包内容；不以这些反馈补写“官网已证实技术栈”。主代理最终对比可另用其样本哈希、文件路径和分析结果支撑这一结论。

## 10. 来源 URL、访问日期与引用能力

### 10.1 第一方证据索引

所有下列采集记录的访问日期均为 **2026-09-08**；本轮收尾未再次联网。锚点来自首页已有 `id`，不表示另一次网络请求。

| ID | 来源 URL | 证据位置／实际获取结果 |
| --- | --- | --- |
| S1 | `http://mianxiaozhu.com.cn/` | 首页完整 HTML；200；品牌、链接、页脚、脚本 |
| S2 | `http://mianxiaozhu.com.cn/#features` | 首页功能区；实时转写、截屏、知识库、技能卡 |
| S3 | `http://mianxiaozhu.com.cn/#how-it-works` | 四步流程；导入、快照、实时辅助、面试后沉淀 |
| S4 | `http://mianxiaozhu.com.cn/#pricing` | 四档价格、模拟面试、Key 说明；另对套餐 HTML 做了只读复核 |
| S5 | `http://mianxiaozhu.com.cn/#trust` | 本地加密、账号访问、训练用途及录屏承诺 |
| S6 | `http://mianxiaozhu.com.cn/#download` | Windows／macOS 下载 href，试用说明 |
| S7 | `http://mianxiaozhu.com.cn/docs` | 200；Swagger UI HTML；明确引用同域 OpenAPI JSON |
| S8 | `http://mianxiaozhu.com.cn/openapi.json` | 200；标题、OpenAPI／info 版本、公开路径和参数描述 |
| S9a | `http://mianxiaozhu.com.cn/robots.txt` | 200；HTML，与同期首页内容完全相同，并非 robots 文本 |
| S9b | `http://mianxiaozhu.com.cn/sitemap.xml` | 200；HTML，与同期首页内容完全相同，并非 XML sitemap |
| S9c | `http://mianxiaozhu.com.cn/help` | 200；HTML，与同期首页内容完全相同 |
| S9d | `http://mianxiaozhu.com.cn/privacy` | 200；HTML，与同期首页内容完全相同，并非独立隐私政策 |
| S9e | `http://mianxiaozhu.com.cn/terms` | 200；HTML，与同期首页内容完全相同，并非独立服务条款 |

`[S9]` 统指 S9a–S9e。失败访问目标另包括 `https://mianxiaozhu.com.cn/` 与 `https://www.mianxiaozhu.com.cn/`，错误详见第 3 节。两条安装包 URL 属于从 HTML 提取的目标，**没有被登记为成功访问的包体来源**。

### 10.2 可复用的 web.run 引用 ID

**无可支持产品事实的有效 web.run 正文引用 ID。** 已按要求实际调用 `web.run` 访问／搜索，但没有得到可引用的官方页面正文。成功证据来自获准的只读 `Invoke-WebRequest` HTTP 抓取及工具返回文本。

因此不虚构 web 引用 ID，也不把抓取错误／搜索失败记录当成产品功能、价格或技术架构证据。上文 S1–S9 是**本报告自定义来源索引，不是 web.run source ID**。主代理可直接复用这些第一方 URL 和本报告摘录；若最终输出必须提供 web 工具渲染引用，需要另行成功获取相应页面，不能冒用不存在的引用。

## 11. 未能核实清单

- “面小猪”与官网“面小助”的名称关系、正式运营主体及完整法律文件。
- 客户端真实版本、发布日期、Windows／macOS 包体状态、官方哈希、签名、文件大小及 CPU／最低 OS 要求。
- 手机端、Web 端、电脑手机协同、跨设备同步；未知不等于没有。
- 实时语音来源、说话人分离、ASR 厂商与语言、端到端延迟及转写准确率。
- LLM／视觉／Embedding 供应商、具体模型、Key 配置兼容列表、Key 存储方式和各套餐的配置权限。
- 模拟面试具体交互、评分报告、音频回放、详细复盘和简历编辑／生成功能。
- 知识库检索实现、存储位置、数据库、向量引擎、导入格式及容量。
- 宣称的本地加密、数据不传云端、不训练和防录屏在软件内是否成立，以及第三方推理数据流。
- 真实付款、授权开通、扣时、时长有效期、退款、发票、设备数和模型调用费用。
- 从官网材料可独立证明的桌面壳和产品后端框架；主代理样本结论须单独引用其静态分析证据。

## 12. 适合主代理复用的保守表述

> 对用户指定的 mianxiaozhu.com.cn，2026-09-08 可取得的 HTTP 官网页面实际品牌为“面小助”。官网宣称提供 Windows／macOS 桌面实时面试辅助、截图解题、个人材料知识库及技能卡，套餐列表包含模拟面试；价格展示为免费 15 分钟、¥9.9／30 分钟、¥99／6 小时，以及需自行配置 API Key 的 ¥199 买断档。官网的隐私和防录屏表述尚未由本次公开资料调查验证。下载按钮目标已从页面 HTML 确认，但本调查没有核验安装包、客户端版本或软件实现框架。